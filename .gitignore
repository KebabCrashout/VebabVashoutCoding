# Installed dependencies - rebuilt by `npm install`
node_modules/

# Per-app local data: products, uploaded images, and API keys.
# Never commit this - data/settings.json holds real API keys.
data/

# Build output - the .exe installers are attached to GitHub Releases instead
dist/

# OS / editor noise
Thumbs.db
.DS_Store
